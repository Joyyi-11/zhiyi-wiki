# 知识库项目规则

> 这是给 AI agent 的运行规则（语义层）。机械工作（索引/校验/回顾）归 `scripts/`，语义工作（摘要/判断/往回织）归本文件。

## 入库流程

收到一个链接或外部材料时，按以下顺序处理：

1. 打开链接，从头到尾读完正文。不得只根据标题、搜索摘要或网页元信息判断内容。
2. 将正文和来源信息完整保存到 `raw/`。原料文件只读，后续不得覆盖或删改；抓取不完整时必须明确报告，不能用摘要冒充全文。
3. 基于完整正文判断内容在讲什么，写成 2-3 句话的摘要和 3-5 个核心要点。
4. 从固定标签体系中选择 3-5 个最贴切的标签；内容不足时可以少选，不为凑数量加入弱相关标签。
5. 将结果保存为 `notes/YYYY-MM-DD-标题.md`（日期用原文发布日期；无发布日期时用入库日期并注明）。
6. 在笔记的 `source_path` 中写入本地原料文件路径；`source_url` 只保存外部来源信息，不能代替本地原料。
7. **材料性质标注**：每条材料必须标注性质——支持（强化既有认知）、补充（填补空白）、修正（纠正局部错误）、反驳（挑战核心判断）、无关。反驳类材料只要论证完整、来源可信，就保留。

## 个人文章入库流程

- `articles/` 保存你已经写成的个人文章原文，是只读事实依据；入库后不得覆盖或删改，也不重复复制到 `raw/`。
- 个人文章笔记使用 `source_type: own_article` 和 `source_path` 标明来源身份与原文位置。
- `source_type` 只表示原料渠道；每篇笔记另用 `knowledge_role` 表示在知识系统中的作用：`personal_basis` 是个人实践与认知基准，`curated_influence` 是你主动整理且影响认知的外部内容，`external_input` 是后来进入知识库、等待比较和吸收的外部材料。
- 除摘要和核心要点外，个人文章笔记还要提取实践背景、实际做法、结果与证据、当时认知和当前状态；个人体验不得包装成普遍事实。
- 编译时以个人经历和当前认知为基准，判断外部材料是在补充、强化、修正还是反驳；历史文章与当前认知不一致时保留认知演变，不覆盖旧表达。
- 编译顺序固定为：先用 `personal_basis` 确定个人问题与基准，再用 `curated_influence` 解释认知来源和演进，最后用 `external_input` 补充、强化、修正或反驳。三类材料共同参与主题综合，但证据身份不能互换。

## 想法笔记流程

- 想法是个人基准的一部分，不是外部资料。`source_type` 用 `own_thought`，`knowledge_role` 为 `personal_basis`。
- 想法没有外部原料，`source_path` 可空；写下来的文字本身就是知识，不得因为缺少来源就放弃记录。
- 想法可简短到一句话；先捕捉，后续在往回织时再补充背景、判断和与其他材料的关系。
- 捕捉入口：`thoughts/inbox.md`（纯文本随时追加，格式自由）或直接由 Agent 写入。`inbox.md` 只是暂存区，不参与索引和编译；只有整理成 `notes/` 下的 `own_thought` 笔记后，才进入 INDEX、往回织和选题。

## 往回织规则

- Agent 先读取 `INDEX.md`，再按新笔记的标题、标签和核心判断定位候选笔记、主题页和实体页；读完候选内容后判断补充、强化、修正、反驳或矛盾。
- 主题页表达当前综合判断，不拼接单源摘要。只有至少两篇笔记形成可复用的共同判断时才新建主题页。
- 笔记的 `entities` 记录文章主角或关键对象；只有同一对象被至少两篇来源讨论，或跨主题承担不同角色时才创建实体页，不为单一来源批量建页。
- 新旧材料矛盾时保留双方证据、时间、适用边界和待验证问题，不强行统一。
- 主题页和实体页必须列出来源笔记；原料层永不改写。
- 完成往回织后运行 `python scripts/build_index.py` 重建 `INDEX.md`。
- 完成整批修改后运行 `python scripts/validate.py`，确认字段、来源和索引结构有效。

### 反确认偏误机制

- **材料性质标注**：每条材料必须标注性质：支持、补充、修正、反驳、无关。
- **主题页强制章节**：所有主题页必须包含「挑战视角」章节，内容包括：如果当前结论错了最可能错在哪里、什么证据会推翻当前判断、哪些场景下当前方法会失效、当前材料集合可能遗漏了哪类声音。
- **问答显性呈现矛盾**：回答问题时，如果相关主题页的「边界与矛盾」或「待验证问题」与当前问题相关，必须在回答中说明；区分高信度判断（多源支持、无重大反例）、有争议判断（存在反例但倾向某方）、开放问题（矛盾未解决）。

## 编译页约定

- `wiki/topics/`：主题页，至少包含当前结论、个人基准、重要认知来源、外部输入、材料如何互相影响、边界与矛盾、挑战视角、待验证问题和来源。
- `wiki/entities/`：实体页，至少包含当前认识、在知识库中的角色、适用边界和来源。
- 核心实体写入笔记 frontmatter 的 `entities`；达到实体页创建条件后，名称与实体页标题一致。
- `INDEX.md` 由 `build_index.py` 完整生成，不手工编辑。

## 问答与选题取用流程

- 问答先读 `INDEX.md`，补充全文定位，优先读取相关主题页和实体页；编译页能够支持回答时，不重新扫描全部笔记或原料。
- 只有核对具体事实、案例、来源差异或矛盾时，才沿编译页的 `sources` 回到相关笔记；只有笔记仍不足时才读取 `raw/`。
- 回答必须区分知识库已经形成的判断、单一来源的经验和仍待验证的问题，并列出实际使用的来源。
- 选题先从主题页的当前结论、材料关系、边界矛盾和待验证问题中生成，再用实体页补充对象角色，最后按需回到笔记提取具体素材。
- 选题以 `personal_basis` 中的经历、认知变化和真实矛盾作为抓手；`curated_influence` 用于解释认知从何而来，`external_input` 主要提供证据、反例和新变化，不能反过来取代个人基准。
- 新选题必须相对于已发表文章和 `personal_basis` 中的核心判断产生认知增量。只是把旧观点换一批外部材料、换标题或补例子，不算新选题。
- 选题至少包含文章角度、核心判断、可用材料和待补证据。没有相关编译页时，明确报告知识缺口，不把单篇笔记包装成综合判断。
- 选题阶段只在对话中生成和比较候选题，不自动写入 `briefs/`；确认值得保留后才保存。
- 问答和选题产生的新判断不自动写回主题页；只有具备跨场景复用价值且经确认后，才进入 Query 回流。
- 回顾流：用 `python scripts/review.py` 生成回顾报告，不绑定周期，想看就跑。默认输出全库状态；传天数参数则附加「近 N 天」切片。回顾只汇总和提醒，不自动改写主题页或笔记。

## 固定标签体系

在下面维护你的标签列表（按你的领域调整）：

`工具使用、方法、实践复盘、概念辨析`

- 标签表示主题或概念，不表示来源格式。
- 不得临时创造近义标签。没有合适标签时记录「标签提案」，但本次不直接加入标签体系。
- 标签必须在读完全文后选择。

## 文件约定

- `raw/`：保存完整原文和来源元信息，是事实依据。
- `articles/`：保存个人文章原文，是个人实践与历史认知的事实依据。
- `notes/`：保存单一来源形成的摘要、要点、标签和当前判断。
- `thoughts/`：保存想法暂存（`inbox.md`），格式自由，不参与索引和编译；整理成 `notes/` 下的 `own_thought` 笔记后才进入系统。
- `wiki/topics/`：保存跨笔记形成的当前主题判断。
- `wiki/entities/`：保存核心对象的当前认识。
- 新笔记的 frontmatter 包含 `entities: []`；Agent 负责维护该字段。
- 文件名中的日期使用原文发布日期；网页未提供发布日期时，使用入库日期并在文件中注明。
- Markdown 文件使用 UTF-8 编码；包含中文的新文件使用 UTF-8 with BOM。
