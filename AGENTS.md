# Zhiyi 仓库规则

`SKILL.md` 是 Zhiyi 的可复用方法论入口，也是安装或迁移到其他 agent 时优先读取的文件。本文件只约束维护当前仓库时的协作边界，不复制完整知识库工作流。

## 维护边界

- 修改方法论、入库流程、往回织规则或安全边界时，优先更新 `SKILL.md`。
- 修改脚本行为时，同步检查 `README.md`、`docs/` 和模板是否仍与实际命令一致。
- `README.md` 面向公开读者，只解释价值、上手方式和仓库结构，不承载完整运行规则。
- `docs/` 负责展开方法说明和使用教程；不要与 `SKILL.md` 大段重复。
- `templates/` 是公开骨架，不放私人知识库内容。
- 不把私人实例中的文章、笔记、实体页、主题页、外部发现游标或个人资料复制进本仓库。

## 验证

完成修改后运行：

```bash
python scripts/build_index.py
python scripts/validate.py
```

如果修改了 `SKILL.md`、模板或脚本，至少确认：

- `INDEX.md` 没有变脏或已按脚本更新。
- `validate.py` 通过。
- README 的快速上手命令仍可执行。
- 新增规则没有把私人实例假设写成通用要求。

## 文件职责

- `SKILL.md`：通用方法论和 agent 工作流。
- `README.md`：公开说明、快速上手和仓库地图。
- `ARCHITECTURE.md`：一页纸数据流和技术选型。
- `docs/`：方法论、入门和自定义说明。
- `templates/`：可复制的笔记、主题页、实体页和选题模板。
- `scripts/`：索引、校验和回顾等机械任务。
